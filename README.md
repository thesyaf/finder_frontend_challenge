# Frontend code challenge application
This is my submission for the frontend code challenge set by Finder.

## Installation

```sh
$ yarn install  #Install project dependencies (or `npm install`)
```
## Once installed, start the server
```sh
$ yarn start
```
